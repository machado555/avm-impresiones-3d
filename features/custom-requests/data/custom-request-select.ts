export const customRequestSelect = `
  id,
  user_id,
  request_number,
  status,
  contact_name,
  contact_email,
  contact_phone,
  request_type,
  priority,
  project_name,
  description,
  quantity,
  material,
  color,
  quality,
  estimated_size,
  estimated_price,
  estimated_date,
  notes,
  internal_notes,
  is_urgent,
  quoted_at,
  approved_at,
  last_status_change_at,
  terms_accepted,
  terms_accepted_at,
  privacy_accepted,
  privacy_accepted_at,
  file_ownership_accepted,
  file_ownership_accepted_at,
  created_at,
  updated_at,
  custom_request_files (
    id,
    request_id,
    file_name,
    file_path,
    file_type,
    file_size,
    is_quote_file,
    uploaded_by,
    created_at
  )
`;
